1. Frontend service:
   http://{{ include "emt-app.frontendServiceName" . }}.{{ include "emt-app.namespace" . }}.svc.cluster.local

2. Backend service:
   http://{{ include "emt-app.backendServiceName" . }}.{{ include "emt-app.namespace" . }}.svc.cluster.local:{{ .Values.backend.service.port }}

3. If Ingress is enabled, set DNS for:
   {{ .Values.ingress.host }}

4. Secrets strategy:
   - local K3D: create {{ include "emt-app.secretName" . }} manually before installing the chart
   - AWS EKS: enable External Secrets and keep real values in AWS Secrets Manager
